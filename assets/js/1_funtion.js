/* Convertir la siguiente función a una función de expresión

function example(a, b, c){
    return a+b+c
} 
    
quedando de la siguiente forma*/

const example = function(a, b, c) {
    return a + b + c;
};

